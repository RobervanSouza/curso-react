/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [ 'procopao.com.br', 'www.patasdacasa.com.br', 'th.bing.com',  ]

  },
}

module.exports = nextConfig
